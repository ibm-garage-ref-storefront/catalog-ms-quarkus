quarkus.elasticsearch.hosts = http://localhost:9200

#optional
quarkus.elasticsearch.username = <user name>
quarkus.elasticsearch.password = <password>

elasticsearch.index=micro
elasticsearch.doc_type=items

ibm.cn.application.client.InventoryServiceClient/mp-rest/url=http://localhost:8081/micro/inventory
